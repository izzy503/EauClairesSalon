@{
  Layout = "_Layout"; // Set the default layout for views in this directory
  ViewData["Title"] = "Eau Claire's Salon"; // Set a default title for views in this directory
  ViewData["CompanyName"] = "Eau Claire's Salon"; // Set a default company name
  ViewData["Year"] = DateTime.Now.Year; // Set the current year
  ViewData["BackgroundColor"] = "#f0f0f0"; // Set a default background color
  ViewData["TextColor"] = "#333"; // Set a default text color
  ViewData["IsLoggedIn"] = false; // Set a default login status
  ViewData["UserFullName"] = ""; // Set a default user full name
}
